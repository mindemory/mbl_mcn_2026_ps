# Secrets — OpenAlex email/API key. NEVER commit. Loaded by enrich_collabs.py.
.env
*.local

# Generated by enrich_collabs.py — cached API responses and audit artifacts.
# collabs.js and mcn_roster.json are intentionally NOT ignored: they are the
# data the static site ships with.
cache/
resolution_review.csv
resolved_authors.json

# Debug run logs
*.log
